import '${inputName}_contract.dart';
import '${inputName}_model.dart';

/// @desc TODO
 /// @time 
 /// @author 
 class ${upperName}Presenter extends CommonPresenter<View, Model> implements Presenter {

   @override
   Model createModel(){
       return ${upperName}Model();
   }
}